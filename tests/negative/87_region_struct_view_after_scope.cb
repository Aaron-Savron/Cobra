struct RequestView: {
    body: readonly []u8,
}

def invalid_region_view_after_scope() -> i64 {
    let request: RequestView
    with region scratch(64): {
        raw = scratch.alloc_u8(8)
        request.body = raw
    }
    return len(request.body)
}
